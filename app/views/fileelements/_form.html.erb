<%= form_for(@fileelement) do |f| %>
  <% if @fileelement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fileelement.errors.count, "error") %> prohibited this fileelement from being saved:</h2>

      <ul>
      <% @fileelement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :filename %><br />
    <%= f.text_field :filename %>
  </div>
  <div class="field">
    <%= f.label :content_type %><br />
    <%= f.text_field :content_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
